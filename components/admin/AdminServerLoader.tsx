// Re-export from the universal ServerLoader for backward compatibility.
// All new code should import from "@/components/ServerLoader" directly.
export { ServerLoader as AdminServerLoader } from "@/components/ServerLoader";
