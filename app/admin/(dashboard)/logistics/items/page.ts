import type { Metadata } from "next";
import ItemsQueuePage from "./ItemsQueuePage";

export const metadata: Metadata = {
  title: "Unchain Summer | Items Queue",
  robots: { index: false, follow: false },
};

export default ItemsQueuePage;
