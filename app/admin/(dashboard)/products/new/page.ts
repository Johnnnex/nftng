import type { Metadata } from "next";
import NewProduct from "./NewProduct";

export const metadata: Metadata = {
  title: "Unchain Summer | New Product",
  robots: { index: false, follow: false },
};

export default NewProduct;
