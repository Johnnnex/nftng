import type { Metadata } from "next";
import NewProduct from "./NewProduct";

export const metadata: Metadata = {
  title: "New Product | NFTNG Admin",
  robots: { index: false, follow: false },
};

export default NewProduct;
