import type { Metadata } from "next";
import ProductsPage from "./ProductsPage";

export const metadata: Metadata = {
  title: "Products | NFTNG Admin",
  robots: { index: false, follow: false },
};

export default ProductsPage;
