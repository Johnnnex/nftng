import type { Metadata } from "next";
import ProductsPage from "./ProductsPage";

export const metadata: Metadata = {
  title: "Unchain Summer | Products",
  robots: { index: false, follow: false },
};

export default ProductsPage;
