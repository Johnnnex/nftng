import type { Metadata } from "next";
import AdminLogin from "./AdminLogin";

export const metadata: Metadata = {
  title: "Unchain Summer | Admin Login",
  robots: { index: false, follow: false },
};

export default AdminLogin;
